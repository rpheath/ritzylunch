<%= title "Announcement" %>

<h2><%= @announcement.title %></h2>
<p class="soft">
  Posted on <%= @announcement.created_at.strftime("%B %d, %Y") %>
  by <%= @announcement.user.name %>
</p>

<%= @announcement.body_html %>