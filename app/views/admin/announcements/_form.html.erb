<%= form.text_field :title %>
<label>By who?</label>
<% if @announcement.new_record? %>
  <p><%= form.lookup_for :user_id, :selected => current_user.id %></p>
<% else %>
  <p><%= form.lookup_for :user_id %></p>
<% end %>
<%= form.date_select :date, :order => [:month, :day, :year], :label => "Announcement Date" %>
<%= form.text_area :body %>