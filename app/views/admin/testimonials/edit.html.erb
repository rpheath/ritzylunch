<%= title "Edit Testimonial" %>

<% form_assistant_for [:admin, @testimonial] do |form| %>
  <%= form.partial 'form' %>
  <p class="submission">
    <%= form.submit 'Update Testimonial' %>
  </p>
<% end %>